
const config = {
	form: {
		storeName: 'authed-support',
		id: 'authed-support',
		pageUrl: `/${process.env.ROUTE_AUTHED}/${process.env.ROUTE_SUPPORT}`,
		apiUrl: process.env.URL_API_SUPPORT,
	},
};

export default config;
