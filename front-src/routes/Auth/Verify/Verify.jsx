import React from 'react';

let Verify = () => {
	return <React.Fragment>
		Verify
	</React.Fragment>;
};

Verify = React.memo(Verify);
Verify.defaultProps = {
};
Verify.propTypes = {
};

export default Verify;
